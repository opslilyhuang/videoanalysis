# DeepSeek API Key（翻译、总结、对话等功能需要）
# 获取地址: https://platform.deepseek.com/
DEEPSEEK_API_KEY=your_deepseek_api_key_here

# OpenAI API Key（仅用于 Whisper 语音转文字：当 YouTube 视频无自带字幕时）
# 获取地址: https://platform.openai.com/api-keys
# 仅线上正式环境建议配置，用于临时上传/转换时无字幕视频的 Whisper 转录
# 不配置时则回退到本地 openai-whisper（需 pip install openai-whisper + ffmpeg）
OPENAI_API_KEY=

# BibiGPT API Token（用于 YouTube 字幕提取，不需要访问 YouTube）
# 获取地址: https://bibigpt.co/user/integration
# 优势：不需要代理，速度快，支持多平台（YouTube/B站/播客）
# 不配置时将跳过此方法，直接尝试其他 fallback 方案
BIBIGPT_API_TOKEN=

# 前端 API 地址（可选）。若出现接口 404，可在 frontend/.env 中设置：
# VITE_API_BASE=http://localhost:8000

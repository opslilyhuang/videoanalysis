# Palantir YouTube 视频分析系统

## 📋 项目概述

这是一个用于分析 Palantir YouTube 频道内容的自动化工具，专门为竞品分析和 AI 深度总结而设计。

## ✨ 核心功能

1. **智能评分系统** - 基于播放量、时效性、业务相关度的三维评分模型
2. **自动等级判定** - S/A/B 三级分类，便于优先级排序
3. **字幕提取** - 支持中英文字幕，包括自动生成字幕
4. **结构化输出** - 为 NotebookLM 优化的文件格式

## 🏗️ 评分模型

### 综合得分公式 (0-100分)

$$Score = V_{score} \times 0.4 + T_{score} \times 0.3 + K_{score} \times 0.3$$

### 维度详解

| 维度 | 权重 | 评分标准 |
|------|------|----------|
| **播放量** | 40% | >100K: 100分, 50K-100K: 80分, 20K-50K: 60分, <20K: 40分 |
| **时效性** | 30% | 2025+: 100分, 2024H2: 80分, 2024H1: 60分, 2023-: 30分 |
| **相关度** | 30% | 核心词: 100分, 次要词: 70分, 通用: 40分 |

### 等级标准

- **S 级 (Strategic)**: Score ≥ 85 - 战略级研究对象，必须拆解
- **A 级 (Active)**: 70 ≤ Score < 85 - 高参考价值，重点关注功能点
- **B 级 (Basic)**: Score < 70 - 基础背景资料，补充语料

## 🚀 快速开始

### 0. 配置环境变量

在项目根目录创建 `.env` 文件（可复制 `.env.example`），配置 DeepSeek API Key：

```
DEEPSEEK_API_KEY=your_deepseek_api_key_here
```

获取 API Key：https://platform.deepseek.com/

### 1. 安装依赖

```bash
pip install -r requirements.txt
```

### 2. 运行分析

```bash
python palantir_analyzer.py
```

### 3. 启动前端与 API（本地开发）

**必须同时运行两个进程**（字幕、智能问答、报告等均依赖后端 API）：

```bash
# 终端 1：启动后端 API（macOS/Linux 使用 python3）
python3 api.py

# 终端 2：启动前端
cd frontend && npm install && npm run dev
```

- 后端默认监听 `http://0.0.0.0:8000`
- 前端默认 `http://localhost:5178`
- 若字幕显示「Failed to fetch」，说明后端未启动，请先运行 `python3 api.py`
- 若出现 `command not found: python`，改用 `python3` 即可

访问前端后，可点击「运行分析」按钮启动分析，进度条会实时显示脚本状态。

### 4. 查看结果

分析完成后，会在 `palantir_analysis/` 目录下生成：

```
palantir_analysis/
├── transcripts/          # 所有视频的独立文本文件
│   ├── [S]_2024-03-15_Palantir AIP Demo.txt
│   ├── [A]_2024-01-20_Tutorial Workshop.txt
│   └── ...
└── video_index.csv      # Excel 可打开的索引文件
```

## 📝 输出文件格式

每个视频的 `.txt` 文件包含：

```
================================================================================
METADATA
================================================================================
Title: 视频标题
URL: https://www.youtube.com/watch?v=xxx
Published: 2024-03-15
View Count: 125,000
Duration: 1800 seconds
Score: 87.5/100
Rank: S (Strategic - 战略级研究对象)

================================================================================
TRANSCRIPT
================================================================================

这里是完整的视频字幕内容...
```

## ⚙️ 配置说明

### 关键词配置

在 `VideoScorer` 类中可以调整关键词列表：

```python
# 核心产品关键词
CORE_KEYWORDS = ['AIPCon', 'Foundrycon', 'Paragon', 'Pipeline', ...]

# 次要关键词
SECONDARY_KEYWORDS = ['Demo', 'Tutorial', 'Workshop', ...]
```

### 字幕优先级

系统会按以下优先级获取字幕：
1. 简体中文 (zh-Hans)
2. 繁体中文 (zh)
3. 英语 (en, en-US, en-GB)
4. 任何可用字幕（包括自动生成）

⚠️ **重要**: 系统已配置 `allow_automated_transcripts=True`，确保能获取到没有人工字幕的 Demo 视频。

## 🔧 常见问题

### Q: 智能报告/字幕提示「接口未找到」或「Failed to fetch」？

**本地开发**：
1. 确保后端已启动：`python3 api.py`
2. 若仍 404，在 `frontend/.env` 中添加 `VITE_API_BASE=http://localhost:8000`
3. 重启前端

**生产部署（用户访问公网 IP 时请求 localhost 失败）**：
- 生产构建必须使用 `frontend/.env.production`（其中 `VITE_API_BASE=` 为空），API 走相对路径
- 若用含 `localhost:8000` 的 `.env` 构建，需删除后重新 `npm run build` 并重新部署

### Q: 如何只测试前10个视频？

修改 `main()` 函数中的 `limit` 参数：

```python
analyzer.analyze_channel(PALANTIR_CHANNEL_URL, limit=10)
```

### Q: 视频没有字幕怎么办？

系统会优先使用 YouTube 字幕，若无字幕则调用 **OpenAI Whisper API** 进行语音转文字。请在项目根目录的 `.env` 中配置：

```
OPENAI_API_KEY=sk-xxx
```

无 API Key 时则跳过语音转文字。

### Q: ffmpeg 缺失怎么办？

下载 YouTube 音频时，若本机已安装 ffmpeg 会得到更好的兼容性。macOS 可执行：`brew install ffmpeg`

### Q: 如何调整评分权重？

修改 `VideoScorer.calculate_score()` 方法中的权重系数：

```python
total_score = (v_score * 0.4) + (t_score * 0.3) + (k_score * 0.3)
```

### Q: 如何分析其他频道？

修改 `PALANTIR_CHANNEL_URL` 为目标频道 URL：

```python
PALANTIR_CHANNEL_URL = "https://www.youtube.com/@其他频道"
```

## 📊 与 NotebookLM 配合使用

1. 将生成的所有 `.txt` 文件批量上传到 NotebookLM
2. 使用 CSV 索引文件快速定位 S 级视频
3. 针对 S 级视频可以单独创建 NotebookLM 实例进行深度分析

## 🚀 部署

### 本地开发与阿里云部署的差异

| 环境 | 后端启动 | 前端 |
|------|----------|------|
| 本地开发 | `python3 api.py`（8000 端口） | `npm run dev`（5178） |
| 阿里云 ECS | systemd 守护进程 | Nginx 托管 `dist/` 静态资源 |

### 阿里云 ECS（前后端一体）

完整步骤见 [DEPLOY_ALIYUN.md](./DEPLOY_ALIYUN.md)：单机部署、Nginx 配置、systemd 守护进程。

### 推送到 Git

```bash
cd /path/to/vedioanalysis
git add .
git commit -m "feat: 多轮问答、临时看板进度与清理、Vercel 部署配置"
git remote add origin https://github.com/你的用户名/vedioanalysis.git   # 若已存在可跳过
git push -u origin main
```

如需忽略大量数据文件（如 `frontend/public/data` 下的字幕与临时看板数据），可在 `.gitignore` 中增加相应规则后再提交。

### 在 Vercel 上线（仅前端）

1. 打开 [vercel.com](https://vercel.com)，用 GitHub 登录并 **Import** 本仓库。
2. **Root Directory**：点击 Edit，选择 **`frontend`**。
3. **Build Command**：`npm run build`（默认即可）  
   **Output Directory**：`dist`（默认即可）。
4. 环境变量（可选）：若线上需要接自有 API，可在 Vercel 项目 **Settings → Environment Variables** 中配置。
5. 点击 **Deploy**，等待构建完成即可获得预览/生产域名。

说明：当前后端 API（`api.py`）需在本地或其它服务器运行；Vercel 仅部署前端静态资源。若希望前后端一体部署，可考虑将 API 改为 Vercel Serverless Functions 或单独部署到 Railway/Render 等。

## 🛠️ 技术栈

- **yt-dlp**: 稳定的 YouTube 元数据获取
- **youtube-transcript-api**: 字幕提取（支持自动生成）
- **pandas**: 数据处理（可选）

## 📄 License

MIT License
